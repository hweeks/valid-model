{
  "name": "valid-model",
  "version": "1.0.1",
  "description": "defaults to validate:true for the options passed to Model.prototype.set",
  "author": "1stdibs.com, Inc.",
  "scripts": {
    "test": "mocha",
    "gitlint": "node_modules/.bin/dibslint --git --warnings",
    "lint": "dibslint --root=."
  },
  "main": "index.js",
  "pre-commit": {
    "run": [
      "gitlint"
    ]
  },
  "devDependencies": {
    "dibslint": "^1.2.4",
    "mocha": "^2.3.4",
    "pre-commit": "^1.1.2",
    "sinon": "^1.17.2"
  },
  "dependencies": {
    "backbone-model": "^1.0.10",
    "lodash.assign": "^3.2.0"
  }
}
